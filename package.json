{
  "name": "@mzero-labs/protocol",
  "version": "1.0.0",
  "description": "Protocol contract ABIs",
  "main": "index.js",
  "author": "mZero Labs <contact@m0.xyz>",
  "license": "GPL-3.0",
  "scripts": {
    "prettier": "prettier --write 'script/**/*.sol' 'src/**/*.sol' 'test/**/*.sol'",
    "solhint": "solhint -f stylish 'src/**/*.sol'",
    "solhint-fix": "solhint --fix 'src/**/*.sol'"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "prettier-plugin-solidity": "^1.1.3",
    "solhint": "^3.4.1"
  },
  "engines": {
    "node": ">=18"
  }
}
